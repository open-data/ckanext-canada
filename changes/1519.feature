Implemented plugin `canada_harvest` which subclasses the `harvest` plugin.

* Added a schema for the `harvest` package type via ckanext-scheming.
* Created a custom harvester class `PortalSync` which replaces the old `post-to-portal` CLI commands.
